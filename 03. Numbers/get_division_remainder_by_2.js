/*
Complete the function getDivisionRemainderBy2 such 
that it returns the division remainder of the number 
it receives by 2. This means that it should return the division 
remainder by 2.
*/


/**
 * @param {string} number
 */
 export function getDivisionRemainderBy2(number) {
    return number % 2;
}
